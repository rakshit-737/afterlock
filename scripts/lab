#!/usr/bin/env sh
# Live lab wrapper. Only for a disposable Docker-capable Linux host.
# Usage: scripts/lab {create|spike|demo|reset|verify|status|destroy} [--repeat N]
set -eu
cd "$(dirname "$0")/.."
exec python3 labs/supervisor/lab.py "$@"
