# Build stage only: the setuptools backend that builds the afterlock project itself
# inside services/api/Dockerfile. It is never copied into the runtime image.
# Installed with `pip install --require-hashes`. Regenerate with:
#   echo 'setuptools==<version>' | uv pip compile --generate-hashes -p 3.11 --no-header -
setuptools==80.9.0 \
    --hash=sha256:062d34222ad13e0cc312a4c02d73f059e86a4acbfbdea8f8f76b28c99f306922 \
    --hash=sha256:f36b47402ecde768dbfafc46e8e4207b4360c654f1f3bb84475f0a28628fb19c
